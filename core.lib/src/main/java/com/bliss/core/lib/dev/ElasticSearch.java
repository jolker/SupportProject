package com.bliss.core.lib.dev;


public class ElasticSearch {
	
	public static void main(String[] args) {
		try {
		/*	GFilter filter = new GFilter();
			filter.setGameId(2);
			// ... 
			filter.setPageNumber(1);
			filter.setPageSize(5);
			PageView<GamePlay> pv= GFilterManager.getInstance().search(filter);
			System.out.println(pv.getItems().size());
			
			filter = new GFilter();
			filter.setValue("fromStartDate", DateUtils.subtractDays(new Date(), 1));
			filter.setValue("toStartDate", new Date());
			pv= GFilterManager.getInstance().search(filter);
			System.out.println(pv.getItems().size());*/
			
		} catch (Exception e) {
			e.printStackTrace();
		}
	}
}
